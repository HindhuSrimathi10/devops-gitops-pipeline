- name: Install dependencies
  hosts: localhost
  become: yes

  tasks:

    - name: Install Docker
      apt:
        name: docker.io
        state: present

    - name: Install Jenkins
      apt:
        name: jenkins
        state: present

    - name: Start Docker
      service:
        name: docker
        state: startedcd
        